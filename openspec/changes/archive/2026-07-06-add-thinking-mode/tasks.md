# Tasks — add-thinking-mode

红灯纪律:headless 内核(provider 类型/能力表/两 wire 映射/两 stream 解析/agent-loop/config/命令解析)强制 TDD、红灯独立成步;**红灯停点 = 2.1**(能力表 + `Depth→请求 body` 映射首次成型,新接口)。TUI 外壳(`TranscriptBlock::Thinking`/事件/折叠/footer)按 `TestBackend`+`insta` 事后快照,不走 red-green。执行 agent MUST NOT:git 写、勾第 9 节真机、全仓 `cargo fmt`(只碰你改的行)、kill 进程、**加新依赖**(thinking 结构用现成 `serde`)。

> **加字段的机械爆炸面(务必按实数处理,勿轻描淡写)**:`ModelRequest` 现**无** `derive(Default)`、`ModelResponse` 有;`Message` 无容器级 serde default。给这三者加字段会命中**大量穷举 struct 字面量**(无 `..rest` 的都编译失败 E0063/E0027)。`..Default::default()` 仅救用了 rest-pattern 的少数站点、且 serde 的 `#[serde(default)]` 只对**反序列化**生效、**不救 Rust 源码构造点**。下列站点数为实读统计,须逐处补字段。

## 1. provider 类型骨架(强制 TDD)
- [x] 1.1 红→绿:`src/provider/mod.rs` 加 `Depth{Off,Low,Medium,High,Xhigh}`(`serde` rename_all lowercase,`Default=Low`)+ `Depth::as_effort(cap:Depth)->&'static str`(封顶降级:xhigh 遇 cap<xhigh 返 "high")+ `ThinkingConfig{depth}` + `ThinkingBlock{text,signature:Option,redacted:bool}`(全 derive `Serialize/Deserialize/Clone/Debug/PartialEq/Default`)。
  - **`ModelRequest` 加 `#[derive(Default)]`**(现仅 `Debug,PartialEq`;四原字段 String/Vec/Vec/Option 均 Default-capable)+ 加 `thinking:Option<ThinkingConfig>`。**`ModelResponse` 已有 Default**,加 `thinking:Vec<ThinkingBlock>`。`DeltaSink` 加 `on_thinking(&self,_:&str){}` 默认空实现。
  - **补齐穷举构造点(实读清单,均无 `..rest`,须逐处补 `thinking:None`/`thinking:Vec::new()`)**:`ModelRequest` ~15 处 —— mod.rs:175、anthropic_wire.rs:92/183、wire.rs:180/239/303、openai.rs:263/565、agent/mod.rs:27/150/266、**compacting.rs:162**、app.rs:394/423/445、anthropic.rs:273/393、mock.rs:82;`ModelResponse` 33 处穷举字面量(遍布 provider/agent/app/tui 各实现与测试,如 mod.rs:165/197/213、wire.rs:113/329、stream.rs:167、anthropic_stream.rs:244、mock.rs:91/149、agent/mod.rs:403/412/421/430/476、compacting.rs:283/344、app.rs 及 tui/mod.rs 各处)。视同一次波及全测试/实现的机械改动,**勿以"仍编译"轻描淡写**。
  - 测试:`Depth` serde 往返、`as_effort` 各档 + xhigh 封顶降级、`ModelRequest`/`ModelResponse` 以 `..Default::default()` 构造不带 thinking 编译且字段空。
- [x] 1.2 红→绿:`src/agent/message.rs` `Message::Assistant` 加 `thinking:Vec<ThinkingBlock>`,**字段级加 `#[serde(default)]`**(保旧 session JSONL 无 thinking 键时反序列化不报 missing field)。
  - **同步全部穷举点(实读:21 构造 + 4 解构 = 25 处,均无 `..`)**:构造 —— app.rs:590/595、agent/mod.rs:166/278(真实 push)+ 781/912/931/1267(测试)、session/mod.rs:241/254/691/703、agent/context.rs:61、compacting.rs:317/322/336/639/698、anthropic_wire.rs:97、openai.rs:268、wire.rs:185,各补 `thinking:Vec::new()`(或按语义 `vec![...]`);解构 —— compacting.rs:88(text 化)、anthropic_wire.rs:20(真实序列化,须绑定并首批回传)、wire.rs:51(真实序列化,OpenAI 忽略)、tui/mod.rs:2900(测试 matches!),补 `..` 或绑定 `thinking`。
  - 测试:`Message::Assistant` serde 往返带/不带 thinking;**向后兼容回归**:反序列化 `{"Assistant":{"text":"done","tool_calls":[]}}`(无 thinking 键)→ 成功且 `thinking==空 Vec`;可加 `store.load()` 层旧会话文件加载成功用例,锁定 `/resume` 兼容。注:"空 Vec 默认"仅对 serde 反序列化成立,对 Rust 构造点须逐处显式补字段。

## 2. 能力表 + 请求映射(强制 TDD;**红灯停点**)
- [x] 2.1 红(**停点**):`src/provider/model_meta.rs` 加 `AnthropicThinking{None,Budget{effort:bool},Adaptive{can_disable:bool,max_effort:Depth}}` + `anthropic_thinking_capability(model)->AnthropicThinking`(静态子串表、最具体在前、未知 claude-\*→`Adaptive{can_disable:true,max_effort:High}`);`OpenAiThinking{None,Effort{max:Depth}}` + `openai_thinking_capability(model)->OpenAiThinking`(o/gpt-5→`Effort{max:High}`、未知→None)。**`max_effort` 取值只能是 Depth 变体**(Opus4.8/4.7/Sonnet5/Fable5→`Xhigh`;Opus4.6/Sonnet4.6 无 xhigh→`High`;不得写不存在的 `Max`)。桩返错误令断言红。测试(红):opus-4-8/sonnet-5→Adaptive can_disable+max_effort=Xhigh、opus-4-6→Adaptive max_effort=High、opus-4-5→Budget{effort:true}、haiku-4-5→Budget{effort:false}、fable-5→Adaptive{can_disable:false}、未知 claude→Adaptive High、gpt-5→Effort、未知→None。**贴测试+失败输出,停下等确认。**
- [x] 2.2 绿:实现两能力表过 2.1 全测(子串匹配 + 保守默认)。

## 3. Anthropic 链(强制 TDD)
- [x] 3.1 红→绿:`anthropic_wire.rs` 加 `anthropic_thinking_body(cap,depth,max_tokens)->(Option<Value>,Option<Value>)` 并接入 `serialize_request`(fn 定义 :7、body 组装 :56):Adaptive+depth≠Off→`thinking={type:adaptive,display:summarized}`+`output_config={effort}`;Off+can_disable→`{type:disabled}`;Off+!can_disable→`output_config={effort:low}`;Budget+depth≠Off→`{type:enabled,budget_tokens:N,display:summarized}`;None→不动。**budget 守卫**:`max_tokens` 为 `None` 或 `<1025` 时 Budget 分支**不发 budget_tokens**(退回省略 thinking),避免 `clamp(_,1024,max_tokens-1)` 在 min>max 时 u32 panic;否则 `N=clamp(max_tokens×ratio,1024,max_tokens-1)`(ratio low.2/medium.5/high.8/xhigh.9)。测试:各能力×各档 body 键值对位、`1024≤N<max_tokens`、**max_tokens=1000/None→不发 budget_tokens**、xhigh 封顶、Off 分型。
- [x] 3.2 红→绿:`serialize_request` Assistant 分支(:20)把 `msg.thinking` 每块作为 content **首批**拼回(`{type:thinking,thinking,signature}` / `{type:redacted_thinking,data}`,排 text/tool_use 前),空则维持现状。测试:带 thinking+tool_use 的 Assistant 序列化 → thinking 块字节一致且居首;无 thinking → 与现状一致。
- [x] 3.3 红→绿:`anthropic_stream.rs` `content_block_start` 识别 thinking/redacted_thinking 建块;`content_block_delta` 加 `thinking_delta`(累积 text+`on_thinking`)与 `signature_delta`(累积 signature);`finish` 推入 `ModelResponse.thinking` 保序。测试:喂 thinking_delta+signature_delta 序列 → `ModelResponse.thinking` 文本/签名正确;redacted 块 `redacted=true`;`on_thinking` 被调。

## 4. OpenAI 链(强制 TDD)
- [x] 4.1 红→绿:`wire.rs serialize_request` 依 `openai_thinking_capability`——**`max_completion_tokens` 与 `reasoning_effort` 解耦**:能力=`Effort`(即 reasoning 模型)→ 输出上限字段**恒**用 `max_completion_tokens`(不论 Depth,含 Off,否则 reasoning 模型见 `max_tokens` 报 400);`Effort` 且 depth≠Off → **额外**发顶层 `reasoning_effort`;能力=`None` → 不动 body(保持 `max_tokens`)。Assistant 分支不发 signature。测试:reasoning 模型 depth≠Off→body 含 `reasoning_effort` 且用 `max_completion_tokens` 无 `max_tokens`;**reasoning 模型 depth=Off→用 `max_completion_tokens` 且无 `reasoning_effort`**;非 reasoning(None)→仍 `max_tokens` 无 reasoning_effort。
- [x] 4.2 绿(可选,无红):`stream.rs apply_data` 解析 `delta.reasoning_content`→`on_thinking`(兼容网关);官方无字段则不触发。测试:喂含 reasoning_content 的 delta→`on_thinking` 被调;不含则不调。

## 5. agent-loop 接线(强制 TDD)
- [x] 5.1 红→绿:`Agent` 加 `set_thinking_depth(Arc<Mutex<Depth>>)`(字段默认 `Low`,**不改 `Agent::new` 签名**)。`run_observed` **两处** ModelRequest 均填 `thinking=Some(ThinkingConfig{depth})`:主循环 :150(loop 顶读快照)与 **forced-final :266**(在 for 外,**须重新读一次 depth 快照**,不能复用循环内已出作用域的局部变量);**两处** `Message::Assistant` push 均带 `response.thinking`:主循环 :166 与 forced-final :278。`run_single_turn`(:27,headless)的 ModelRequest 仅补 `thinking` 字段(置 `None`)以过编译,不必注入。不设 tool_choice。测试:mock 断言主循环轮 + **触顶 forced-final 轮**请求均带 depth、response.thinking 入 history 且下一轮请求原样回传;默认 Low 时既有 agent-loop 测试零回归。
- [x] 5.2 红→绿:`/model` 切换(`set_model`/`SetModel` 路径)清空 caller `history` 内所有 `Message::Assistant.thinking`(避免跨模型 signature 在带 tool_use 多轮回传触发 Anthropic 400)。测试:history 含带 signature 的 Assistant thinking,`/model` 切换后该字段为空 Vec、其余字段不变。

## 6. config(强制 TDD,照 keep_recent_turns 先例)
- [x] 6.1 红→绿:`Config.thinking:Depth` + `RawConfig.thinking:Option<Depth>`(`#[serde(default)]`);`merge` 加 `project.thinking.or(user.thinking)`;`resolve` 填 `DEFAULT_THINKING=Depth::Low`。测试:user/project 各设 + 覆盖 + 缺省=Low。

## 7. /think 命令(强制 TDD)
- [x] 7.1 红→绿:`command.rs` 四处同改:`Command::Think(ThinkArg)`(`ThinkArg = Query | Set(Depth) | Invalid(String)`——因 `parse_command` 返 `Option<Command>` 无错误通道、`Depth` 闭合枚举容不下非法串,`/think foo` 须落 `Invalid("foo")` 由 dispatch 出 Notice 列合法取值、不改档)、`BuiltinCommand::Think`、`COMMANDS[_;8]`(name `/think`、usage `/think [off|low|medium|high|xhigh]`、desc)、`parse_command`(裸=Query、`/think <depth>`=Set、`/think <非法>`=Invalid)。测试:`/think`→Query、`/think high`→Set(High)、`/think foo`→Invalid("foo");`command_metadata_covers_all_builtin_commands_and_matches_parser`(实名带后缀)的 `expected` 名字数组 + `COMMANDS` 长度同步到 8。

## 8. 接线 + TUI 外壳(注入接线强制 TDD;TUI 展示 insta 事后)
- [x] 8.1 红→绿:**仿 `set_permission_mode` 真实先例接线**(注入点在 `src/tui/mod.rs run_tui`、**非** `assemble_agent` 内,`assemble_agent` 签名与 `src/app.rs` 一行不动):于 tui/mod.rs 以 `config.thinking` 为初值建 `Arc<Mutex<Depth>>`(置于 assemble_agent 调用前,仿 :109)→ 返回后 `assembled.agent.set_thinking_depth(depth.clone())`(仿 :128)→ `state.thinking_depth=depth` 存档(仿 :164)。其余 assemble_agent 调用点(cli.rs:551/e2e.rs:97/各 tui 测试)默认 Low、零改。测试:注入后 depth 源生效、config 初值传入。
- [x] 8.2 TUI(insta 事后):`channel.rs AgentEvent::ThinkingDelta(String)` + `ChannelSink::on_thinking` 发之;`app.rs TranscriptBlock::Thinking(String)` + `apply()` 分支仿 `TextDelta` 累积;`/think` 命令分发改共享 depth(`Invalid` 出 Notice 列合法取值);footer 档位指示。`render.rs thinking_block_lines` **默认展开**渲 `text_muted` 灰字正文、**仅折叠溢出尾部**:`const THINKING_FOLD_THRESHOLD=12`,渲染后行数 L≤12 全显、L>12 显前 12 行 + `… +{L-12} 行(Ctrl+O 展开)` 标记,`tools_expanded` 真则全显;always-on 模型 Off 出"思考无法关闭"notice。暗/亮双主题 insta 快照锁定(短思考全显、长思考折叠溢出、Ctrl+O 展开、footer 档位、always-on notice)。

## 9. 门禁 + 真机(真机主 agent / 用户;执行 agent 勿勾)
- [x] 9.1 `cargo test --lib` 全绿(含 1.2 旧 session 向后兼容回归);`cargo clippy --all-targets -- -D warnings` 零警告;`cargo build`(exe 占用隔离 `CARGO_TARGET_DIR`);`openspec validate add-thinking-mode --strict`;`git diff Cargo.toml` 无新依赖;既有非-thinking 快照零 churn、涉 assistant/footer 的快照 churn 已 review。
- [x] 9.2 真机:Anthropic(Opus/Sonnet)`/think high`→回带思考的 md,TUI 折叠/展开见灰字思考正文、footer 显 high;`/think off` 关思考(Sonnet 5 显式 disabled)不再出思考块;带工具调用的多轮不 400(原样回传生效);**`/model` 切换后不 400**(剥离生效);OpenAI/WPS `/think medium`→请求带 reasoning_effort 不报错(思考区可空);`/think off` 下 reasoning 模型仍不 400(max_completion_tokens 生效);切档即时生效;暗亮主题各看一眼。
