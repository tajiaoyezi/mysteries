## 1. 发布基线与范围冻结

- [x] 1.1 记录当前 `master == origin/master`、工作区状态、根 package/lockfile=`1.2.0`、`CHANGELOG.md` 两条 v1.3 `[Unreleased]`、现有 tag/Release仅有v1.2.0，以及远端不存在`v1.3.0` tag/draft/public Release；同时记录repository immutable releases=`enabled:false`、v1.2.0 Release `immutable:false`、rulesets为空、master未保护且尚无受保护`release` environment。任一事实漂移先修订proposal/design/spec。
- [x] 1.2 核对`add-agent-execution-scope`与`add-readonly-subagent`均已archive、tasks/真机验收完整，确认v1.3.0只收口已完成能力，不新增subagent行为、dependency、config/session wire或Rust API。
- [x] 1.3 统计并保存版本敏感基线：当前包含`v1.2.0`的tracked TUI snapshot文件数为71，`src/tui/mod.rs`中两个`app_version: "1.2.0"`属于session fixture而非package-current输出；后续只迁移`env!("CARGO_PKG_VERSION")`驱动的snapshot文本。
- [x] 1.4 静态核验`.github/workflows/release.yml`已从根version动态派生metadata、archive、bundle与公开URL，并记录四项已确认drift：publish job缺少直接event/ref gate；不会机器验证同SHA的CI/Security/dry-run；remote assets只用remote checksum自洽验证而不与sealed bundle比对；公开API不要求`immutable=true`。逐项核对PR/dispatch/tag事件、job权限、固定runner/toolchain、Action完整SHA和draft-to-public边界。
- [x] 1.5 重新核验固定Rust`1.96.1`、`windows-2022`/`ubuntu-22.04`、workflow引用的Action SHA、GitHub Actions runs/attempt-specific jobs、Git ref/Compare、branch/tag rulesets、protected environment与immutable releases API/Release `immutable`字段仍可用；明确immutable setting读取需要admin read、不得把该权限交给workflow。锁定required check contexts精确为`fmt · clippy · test · build (ubuntu-latest)`、`fmt · clippy · test · build (windows-latest)`、`RustSec dependency audit`，并从当前check-runs确认三者GitHub Actions `app.id=15368`；release jobs精确为`Validate release metadata`、`Package release (linux-x86_64)`、`Package release (windows-x86_64)`、`Assemble release bundle`、`Publish GitHub Release`、`Verify published release (windows-x86_64)`、`Verify published release (linux-x86_64)`。现场API、app.id或名称若已失效，停止并先修订design/spec，不得静默使用模糊匹配、floating `stable`、`*-latest`、浮动Action tag、PAT或长期credential。

## 2. 固化 v1.3.0 版本与发布文档

- [x] 2.1 将根`Cargo.toml` package version改为`1.3.0`，用Cargo更新`Cargo.lock`；证明lockfile只改变根`mysteries` version，不执行无界`cargo update`、不改变dependency解析。
- [x] 2.2 在`CHANGELOG.md`保留新的空`[Unreleased]`，把Agent execution scope与单层只读`delegate_task`固化为`[1.3.0] - <计划annotated tag创建UTC日期>`；保留v1.2.0及更早内容/链接，新增v1.3.0 tag/compare链接。tag前若UTC日期已变化，必须另走日期修正PR与完整门禁。
- [x] 2.3 审查v1.3.0 notes：明确已交付scope/cancellation/capability/canonical workspace只读委派，同时继续披露无per-response delegate occurrence硬上限、跨child token总预算、child-only扫描字节硬上限、递归、后台任务、child session及写入/Network child；不虚构config/session迁移或总资源硬上限。
- [x] 2.4 把README中Agent execution scope与单层只读`delegate_task`从`[Unreleased]`固化为v1.3.0，确保随正式archives打包的README准确描述所含能力；Windows/Linux安装命令则先从公开`releases/latest`解析真实stable tag，再由该tag派生versioned archive、`SHA256SUMS`、解压目录和`--version`验证，implementation PR阶段不得硬编码尚未公开的v1.3.0 URL。保留v1.2.0历史事实及源码构建路径，并用当前public v1.2.0现场执行命令证明无404。
- [x] 2.5 核对`deliverables/README.md`继续把本目录定义为演示/历史验证产物而非当前binary渠道，指向GitHub Releases与根README安装说明并保留源码构建路径，不要求在该文件重复完整下载/checksum/解压命令，不添加或改写历史binary；当前内容满足该边界时保持零diff。
- [x] 2.6 新建精简`manual-verification.md`：只列实现阶段自动门禁、PR bundle复核、repository setting授权、master dry-run、tag授权、protected environment deployment approval、公开asset与Windows TUI真机步骤；setting部分明确immutable releases、`protect-master`、`protect-stable-tags`与`release` environment的独立授权/核验，deployment部分给出“打开精确tag run→Review deployments→选择`release`→Approve and deploy”的最短UI路径并说明tag授权不得复用。所有命令先用`git rev-parse --show-toplevel`定位repository，可在active或archive change路径下直接复制，使用占位符而不写真实run/tag/Release证据。
- [x] 2.7 加固`.github/workflows/release.yml`：publish job的`if`同时要求event=`push`、完整ref位于`refs/tags/`、metadata `publish=true`且`github.run_attempt == 1`，使tag workflow任何rerun的publish job在environment与写token前skip；job绑定`release` environment，权限精确为`contents: write`+`actions: read`，其他权限为none。canonical SemVer与annotated tag继续由metadata shell校验；tag-triggered metadata另从annotated tag object读取tagger timestamp并转UTC日期，要求等于唯一Changelog version heading日期。明确修正现有两处`peeled == master tip`：metadata已checkout完整history，fetch受保护`origin/master`并执行`git merge-base --is-ancestor "$REVISION" origin/master`；publish不得checkout或执行任何checkout-derived repository source/script/binary，在仅该API step设置`GH_TOKEN`并执行最多三次稳定读取循环：GET `heads/master`得`MASTER_BEFORE`，用精确`REVISION...MASTER_BEFORE`调用Compare并要求`base_commit.sha=REVISION`、`merge_base_commit.sha=REVISION`、`status in {ahead, identical}`、`behind_by=0`，再GET `heads/master`得`MASTER_AFTER`，两次SHA相同才通过，否则重试，三次均变化则fail-closed；不得读取Compare不存在的`head_commit`字段。禁止漏改任一处、保留tip equality、让publish依赖本地Git graph或只检查Compare status。package/aggregate构建路径与CI/Security workflows保持不变。
- [x] 2.8 在publish job首次Release API写入前增加只读evidence preflight：再次断言`GITHUB_RUN_ATTEMPT=1`，先读取当前`release` environment ID/name，再通过`/actions/runs/{GITHUB_RUN_ID}/approvals`断言存在`state=approved`、`user.login=tajiaoyezi`且`environments`包含该精确ID/name的review，并拒绝同environment的任何非approved review；仅用户名、其他environment或空history不得通过。再读取当前tag run的server-side `created_at`，按workflow file/ID、event、`head_sha`与`head_branch`筛选同`REVISION`的master `ci.yml` push、`security-audit.yml` push和`release.yml` workflow_dispatch，并按每个run的`run_attempt`枚举attempt-specific jobs endpoint。run-level `status`/`conclusion`不得作为过滤条件；CI job集合必须且仅有`fmt · clippy · test · build (ubuntu-latest)`与`fmt · clippy · test · build (windows-latest)`两个success，Security必须且仅有`RustSec dependency audit`一个success，dry-run必须有metadata、两个package、assemble四个success及publish、两个public verify三个skipped，名称逐字等于1.5锁定值。每类允许多个run/attempt，但只有全部预期jobs的server-side`completed_at`严格早于tag run`created_at`者合格，并按`(max(job.completed_at), run_id, run_attempt)`降序选择首项、记录精确`run_id/run_attempt`；所选attempt内部预期job名称/结论必须唯一，较晚rerun不得抹掉较早有效attempt。再验证environment reviewer=`tajiaoyezi`、`prevent_self_review=false`、`custom_branch_policies=true`，分页读取deployment-branch-policies并要求总数为1且唯一name精确`v1.3.0`；runtime不得依赖只读schema未承诺的policy type字段，`type=tag`由4.6的admin证据承担。以`includes_parents=false`列出本repository rulesets，按精确name各唯一定位`protect-master`与`protect-stable-tags`并GET by ID，通过显式canonical projection验证`source_type=Repository`、`source=$GITHUB_REPOSITORY`、target、active enforcement、`conditions.ref_name.include/exclude`及rules等于4.6批准配置：无序数组排序、rule type集合精确相等；`pull_request.dismissal_restriction`仅允许缺失或`{enabled:false,allowed_actors:[]}`，`required_reviewers`仅允许缺失或空数组，`required_status_checks.do_not_enforce_on_create`缺失按`false`归一化，tag target的`update.parameters.update_allows_fetch_and_merge`仅允许缺失或`false`；其他documented enforcement字段逐项精确比较。可选默认字段若enabled/非空、tag update参数为`true`、额外rule type或未知security-affecting parameter必须fail-closed；rule外response metadata不参与等价判断。`bypass_actors`可能对无ruleset写权限token隐藏，runtime不得把字段缺失当空值；无常驻bypass由4.6的admin API/UI证据证明。禁止把继承ruleset、模糊名称、最新run或branch tip当作匹配；`GH_TOKEN`只绑定这些只读API/Compare/Release steps，且job没有`actions: write`或`administration`。
- [x] 2.9 加固draft identity：按asset ID下载后，`SHA256SUMS`与两个archives逐字节等于本run sealed bundle，再用本地sealed manifest校验remote archives；draft API `.body`经JSON UTF-8解码后逐字节等于sealed `release-notes.md`，不得做换行/空白规范化；API若提供digest则同步核对。公开PATCH前再次匿名重验remote tag object/peeled ref；公开后API必须断言`immutable=true`且public `.body`仍与sealed notes逐字节相等。
- [x] 2.10 更新publish失败处理：tag一旦push，tag workflow任一步失败/取消都立即烧掉当前version/tag，不区分零diff或transient；保留失败run、tag和残留非公开draft，禁止删除、移动、重建、公开或rerun复用。缺失pre-tag evidence、tag前immutable/environment/ruleset未成立、任何批准事实漂移或需要repository/workflow/package/notes diff均只能走新的patch release change。仅在attempt 1仍执行且candidate为ancestor、immutable/environment及两组rulesets保持批准事实时，master通过受保护PR正常前进不算漂移。

## 3. 版本文本 snapshots 与本地质量门

- [x] 3.1 运行版本相关CLI/TUI测试生成v1.3.0 snapshot候选；只接受71份既有snapshot中的`v1.2.0`→`v1.3.0`字面量变化，逐文件证明metadata、颜色token、布局、换行与其他正文零diff，`.snap.new`为0。
- [x] 3.2 保持Rust source零diff；尤其不得机械修改`src/tui/mod.rs`中作为session fixture的两个`"1.2.0"`。若真实编译/测试要求修改runtime source，停止并重新判定TDD与change范围。
- [x] 3.3 验证`cargo metadata --locked`、根`Cargo.toml`、根`Cargo.lock`、Changelog唯一带日期heading与release notes均精确解析为`1.3.0`，`cargo tree --locked -p mysteries@1.3.0 --depth 0`成功。
- [x] 3.4 运行`cargo fmt --all -- --check`、`cargo clippy --all-targets --locked -- -D warnings`、`cargo test --locked`全量lib/integration、`cargo build --release --locked`及release binary `--version`/`--help`，记录通过数与ignored。
- [x] 3.5 运行固定`cargo-audit audit --deny unsound --file Cargo.lock`，要求0 vulnerability/0 unsound并如实保留allowed unmaintained warning；不得新增ignore或改变Security policy。
- [x] 3.6 运行release metadata/bundle/evidence本地静态与负向验证，覆盖非canonical/lightweight tag、annotated tagger UTC日期与Changelog不一致、tag publish `run_attempt != 1`、版本或Changelog不一致、错误/缺失/额外archive、空文件、symlink、checksum不匹配、remote manifest+archive同时替换、draft或public API `.body`与sealed notes存在正文/编码/换行/尾随空白差异、CI/Security/dry-run缺失/失败/错误SHA/错误event/错误或额外job名称/集合、所有合格attempt均在tag run创建后才完成、所选attempt内部重复job、错误或缺失`run_attempt`、approval空history/非approved state/错误user/错误environment ID或name、environment缺失/错误reviewer/self-review/tag policy、ruleset缺失/重复/被继承ruleset冒充/错误name/source/source_type/active/target/include/exclude/rule type/rule parameter/required context integration_id/额外rule、可选review restriction字段enabled或非空、tag update参数为`true`、未知security-affecting parameter、两次master ref读取变化且三次不稳定、Compare错误base/merge-base/status/behind_by、误读不存在的`head_commit`、candidate不再是master ancestor，以及公开前tag漂移；另有正向case证明ruleset数组不同顺序可规范化为同一contract，缺失与GitHub回显的`dismissal_restriction={enabled:false,allowed_actors:[]}`、缺失与空`required_reviewers`、缺失与`false`的`do_not_enforce_on_create`、tag `update`缺失参数与`update_allows_fetch_and_merge=false`分别归一化为同一安全contract，多个合法dry-run/attempt按降序tuple确定性选中一个、较晚失败或仍运行的evidence rerun及其run-level `status`/`conclusion`不覆盖较早有效attempt、API隐藏`bypass_actors`不被误判为空或导致永久失败、master稳定且candidate为ancestor时Compare返回ahead并通过、前一次master读取变化但后续稳定时能在三次内重试成功。所有失败均在首次Release API写入或public PATCH前fail-closed且不访问真实credential。
- [x] 3.7 静态权限测试证明PR/dispatch无publish部署，只有publish job拥有`contents: write`+`actions: read`并绑定`release` environment；`GH_TOKEN`只存在于当前repository的Actions/environment/rulesets/Git ref/Compare/Release API steps，不进入checkout、artifact download、checksum、identity或public verify。确认publish仍不checkout或执行任何checkout-derived repository source/script/binary，CI/Security workflow零diff且无`actions: write`、`administration`、PAT、长期credential、`id-token`或attestations权限。
- [x] 3.8 运行`openspec validate release-v1-3-0 --strict`与`openspec validate --all --strict`、`git diff --check`及scope扫描；确认只有proposal允许的version/docs/snapshots/change artifacts与有明确依据的release workflow修复，无binary/archive/checksum、凭据或绝对用户路径。

## 4. Implementation PR 验证与合入

- [x] 4.1 在任何Git写操作前展示最终diff、71份snapshot机械变化、本地门禁与远端无v1.3.0对象；取得用户对implementation commit/push/PR的批准后再操作，此批准不包含tag创建。
- [x] 4.2 创建implementation commit/branch/PR，确认release-sensitive path触发CI、Security audit与Release validation；PR期间远端仍无`v1.3.0` tag、draft/public Release或Release assets。
- [x] 4.3 验证PR Windows/Ubuntu CI、RustSec、release metadata、Windows/Linux package与aggregate全部success；三个checkout job的唯一`RELEASE_REVISION` marker等于现场synthetic merge ref SHA。通过Actions API断言run `event=pull_request`且`head_sha`等于PR head SHA，再用已知PR number的持久PR endpoint断言`.head.sha`相同；run `pull_requests[]`只记录观察值、为空不得阻断。publish/public verify skipped/absent且未请求environment approval。
- [x] 4.4 下载PR run的`release-bundle-1.3.0` artifact，离线验证精确包含两个versioned archives、`SHA256SUMS`与release notes，checksums与archive根文件集正确；明确它只是workflow artifact，不是公开GitHub Release。
- [x] 4.5 对version/docs/spec/snapshot与任何workflow diff做独立对抗式审查，要求无P0/P1/P2、snapshot无非版本churn、Actions evidence/asset identity/失败分流完整且发布权限未退化后才允许进入setting配置与merge。
- [x] 4.6 向用户展示当前`enabled:false`、rulesets为空及无protected environment事实，说明GitHub immutable releases只在Release公开后锁定assets/tag并自动产生platform release attestation；同时展示`release` environment（reviewer=`tajiaoyezi`、`prevent_self_review=false`、`custom_branch_policies=true`且唯一policy为`name=v1.3.0,type=tag`、UI关闭admin bypass）及以下精确ruleset JSON语义与回滚影响：`protect-master`为repository source `tajiaoyezi/mysteries`、`target=branch`、`enforcement=active`、include仅`refs/heads/master`且exclude空，rules仅为`pull_request`（`allowed_merge_methods=[merge,squash,rebase]`、`required_approving_review_count=0`、`dismiss_stale_reviews_on_push=false`、`require_code_owner_review=false`、`require_last_push_approval=false`、`required_review_thread_resolution=false`，不配置dismissal restriction或beta required reviewers）、`required_status_checks`（三个1.5精确contexts且各`integration_id=15368`、strict=true、do_not_enforce_on_create=false）、`deletion`、`non_fast_forward`；`protect-stable-tags`为同repository source、`target=tag`、active、include仅`refs/tags/v*`且exclude空，rules仅为无参数语义的`update`、`deletion`；两者`bypass_actors=[]`且不得有额外rules。另获repository admin mutation明确授权后执行设置，通过admin/只读API重新读取`enabled:true`、environment protection、分页deployment policy列表及两组rulesets的精确name/source/source_type/target/enforcement/conditions和canonical rules contract；GET若回显disabled/empty `dismissal_restriction`、空`required_reviewers`、省略默认false的`do_not_enforce_on_create`，或tag `update`省略branch-only参数/回显`update_allows_fetch_and_merge=false`，按2.8规则归一化后验证，不要求raw JSON字节相等。两组ruleset无常驻bypass、policy `type=tag`和REST未暴露的admin-bypass开关以admin API/UI截图或审计记录核验。该批准不包含implementation merge或tag；后续stable release需另行授权把environment policy切到自己的精确tag。
- [x] 4.7 设置验证后，向用户展示implementation PR的精确head/merge candidate、全部checks、独立review与settings重读结果，另获“合入该精确PR，并在merge成功后立即以`ref=master`dispatch release dry-run”的明确批准；setting批准不得复用，批准内容必须同时覆盖merge与紧随其后的远端workflow dispatch，但不包含tag。获批后合入PR并记录唯一merge SHA，证明merge遵守active `protect-master` ruleset；合入后把该SHA冻结为release candidate，并再次确认immutable releases/environment/两组rulesets仍生效且远端仍无v1.3.0 tag/Release。合入后立即进入5.1，在dry-run run ID及其`head_sha`锁定前不得允许其他PR推进master。

## 5. Master 精确门禁、dry-run 与 tag 授权

- [x] 5.1 在4.7的merge+dispatch明确授权范围内，implementation merge后先确认该SHA仍是`origin/master` tip，立即以GitHub支持的`ref=master`触发`release.yml` workflow_dispatch dry-run，唯一定位新run并记录run ID，要求其`event=workflow_dispatch`且`head_sha`精确等于candidate；不得把raw SHA误作dispatch ref。在该run identity锁定前不得等待其他门禁或允许master前进；若4.7未明确覆盖dispatch则不得合入，不能在此处临时复用setting批准。
- [x] 5.2 唯一定位implementation merge SHA对应的master Windows CI、Ubuntu CI与Security audit，并与5.1已锁定的dry-run一并等待完成；要求CI/Security全部success且revision marker精确等于该SHA，dry-run metadata、Windows/Linux package与aggregate全部success而publish/public verify不运行，远端仍无v1.3.0 tag/Release。不得复用PR head/synthetic merge、其他master commit或不同head SHA的dispatch；此后master正常前进不使candidate证据失效。
- [x] 5.3 下载并复核master dry-run的`release-bundle-1.3.0`，确认version、release notes语义、archive文件集及各run内部checksums与PR验证规则一致且run head SHA等于候选merge SHA；不要求不同runner/run生成bit-for-bit相同archive digest。
- [x] 5.4 向用户展示candidate SHA、三类证据将被确定性选择的精确`run_id/run_attempt`及所有预期job完成时间、dry-run bundle、immutable/environment/branch+tag ruleset状态、Changelog日期、annotated tag对象模型、三个公开asset名称，以及“master可前进但tag永远绑定candidate”的失败边界；确认所有证据jobs已完成后，重新取得为该精确SHA创建并push`v1.3.0` tag的明确授权，implementation/setting批准不得复用。
- [x] 5.5 获批后，在创建tag前验证当前UTC日期等于Changelog v1.3.0 heading、candidate仍可从受保护`origin/master`到达、`protect-stable-tags`精确contract仍active且远端tag不存在；日期或可达性不一致时停止并走修正PR/新授权。创建本地annotated`v1.3.0`后读取tagger timestamp并转换为UTC，要求再次等于heading，且peeled commit精确等于candidate SHA；不一致时不得push并移除未发布本地tag。push后匿名重新读取完整tags列表，验证tag object与`^{}` peeled ref各唯一且SHA不同、peeled SHA等于candidate；master tip可不同但candidate必须仍为ancestor，禁止force/move/delete已有remote tag。tag workflow metadata必须独立重复tagger UTC日期检查，不能只信人工证据。
- [x] 5.6 唯一定位新tag run并等待其publish job进入`release` environment待批准状态；向用户展示精确run ID/attempt、candidate/tag peeled SHA、已完成metadata/package/assemble结果、5.4的pre-tag evidence及immutable/environment/两组rulesets的admin重读结果，另获对本次`release` deployment的明确批准后才在GitHub UI执行Approve and deploy。tag创建/push授权不得复用为deployment approval；用户拒绝或保护事实漂移时保持等待或停止，绝不bypass。

## 6. Tag 发布与公开下载验收

- [ ] 6.1 在5.6明确批准并执行deployment后，唯一定位`v1.3.0` tag-triggered Release run，要求`run_attempt=1`，`/actions/runs/{run_id}/approvals`记录`state=approved`、`user.login=tajiaoyezi`及当前`release` environment精确ID/name且没有同environment非approved记录或admin bypass，metadata、Windows/Linux package、aggregate、publish及两个public verify jobs全部success；publish日志记录tag run `created_at`与确定性选中的CI/Security/dry-run `run_id/run_attempt`，证明每个所选attempt的全部预期jobs更早完成并精确绑定candidate SHA，environment及两个精确ruleset contract匹配，所有checkout revision markers、run head SHA及远端peeled tag一致，metadata证明tagger UTC日期与Changelog相等且candidate仍是受保护master的ancestor；publish的稳定master ref前后SHA、精确Compare请求、`base_commit`/`merge_base_commit`、`behind_by=0`与ahead/identical状态共同证明candidate仍可从当前master到达。
- [ ] 6.2 通过公开GitHub Release metadata验证`tag_name=v1.3.0`、`target_commitish`非空且仅作观察值、`immutable=true`、draft/prerelease=false、latest指向本Release，并重新解析远端tag peeled commit等于候选merge SHA；公开Release只含两个非空versioned archives与`SHA256SUMS`三个assets，draft下载证据证明三者逐字节等于本run sealed bundle，且draft/public API JSON解码后的`.body`均逐字节等于sealed `release-notes.md`。
- [ ] 6.3 匿名重新下载Windows ZIP与`SHA256SUMS`，验证checksum、根文件集、`mysteries.exe --version == mysteries 1.3.0`及`--help`；不得依赖GitHub token或本机credential。
- [ ] 6.4 匿名重新下载Linux tar.gz与`SHA256SUMS`，验证checksum、根文件集、executable bit、`mysteries --version == mysteries 1.3.0`、`--help`与GLIBC≤2.35；可使用tag workflow public verify作为权威Linux证据。
- [ ] 6.5 用户在Windows Terminal从公开Release ZIP启动TUI并正常退出，确认PowerShell立即恢复输入且测试不污染真实credential/session；该真机项未通过前不得archive。
- [x] 6.6 若tag workflow任一步失败或被取消，保留run、tag和可能的非公开draft并停止，立即把v1.3.0标记为已消耗；不得删除、移动、重建、公开tag/draft或rerun同一tag，即使失败为零diff/transient且candidate、immutable、environment与rulesets未漂移。记录失败边界，后续另起v1.3.1（或下一patch）change；若Release已公开，任何缺陷同样只能走patch release。

## 7. 证据收口与 OpenSpec 归档

- [ ] 7.1 汇总implementation PR run `head_sha`与持久PR endpoint、immutable/environment/`protect-master`/`protect-stable-tags` admin状态与required-review deployment、tag run server `created_at`、被选CI/Security/dry-run `run_id/run_attempt`及其预期jobs完成时间、candidate对master的可达性、tag object/tagger UTC/peeled SHA、tag run/jobs、Release ID/`tag_name`/`target_commitish`观察值/`immutable=true`、sealed-to-remote assets与draft/public body identity、asset names/sizes/checksums、Windows/Linux smoke和Windows TUI真机证据；以peeled tag而非`target_commitish`证明revision，确认全部权威证据指向同一v1.3.0 release commit且不含凭据/绝对用户路径。procedure文件保持placeholders，持久化证据统一交给7.3的archive决策记录。
- [x] 7.2 重新运行OpenSpec strict、diff/scope与远端事实检查；依据真实证据完成剩余tasks，不向v1.3.0 tag revision追加递归evidence commit。
- [ ] 7.3 起草`.ai_history/logs/` archive决策记录，包含最终定案、被否决的“发布前继续扩功能/复制workflow/移动tag”等备选及关键远端证据；同时准备README v1.3.0历史/Release链接、sync后的`release-delivery`/`dependency-security`、最终tasks与change移动的精确post-release diff，交用户审阅。现场证明动态`releases/latest`命令已解析到v1.3.0，并确认tag archives内README已在implementation阶段把本次能力标记为v1.3.0而非`[Unreleased]`；未获批准不得创建branch、commit、push或PR，该docs diff不得回写tag revision。
- [ ] 7.4 用户明确批准7.3的精确archive diff及branch/commit/push/PR操作后，创建post-release docs/archive branch，在同一commit写入获批决策记录、README链接、同步后的specs、最终tasks并移动change，随后push并创建archive PR；该批准不包含PR merge。
- [ ] 7.5 验证archive PR checks与独立审查无P0/P1/P2，向用户展示精确PR/merge SHA预期并另获merge明确批准后才合入。archive merge使`master`领先`v1.3.0` tag属于预期，不得移动tag追随archive。
