# 03 · 组件清单(Component Catalog)

> 每条:数据来源(源码字段)· 状态 · 驱动事件 · 终端渲染 · 三分类。
> 驱动事件对应技术方案 §3 的 `AgentEvent` / `AgentStatus`。
> 约定:`port ✅ / adapt ⚠️ / drop ❌`。所有卡片**圆角 ❌、描边 ⚠️**(box-drawing 或留白)。

### C1 · 顶栏 header
- 内容:`✦ mysteries  agent · v1.0`(仅品牌,左对齐,dim)。**原型右侧的 `provider / model` 不在顶栏渲染**——已归状态行,避免重复(D1 决议,见 `02` 与技术方案 §8)。
- 渲染:1 行。✅

### C2 · 欢迎态(空会话)
- 触发:`blocks` 为空。
- 内容:`✦ MYSTERIES`(wordmark)· `AGENT · v1.0 · 终端编码助手` · 标语「读只读,写必询 —— 每一次文件改动与命令执行,都先把 diff 摊给你,等你按下 `y` 才动手」· `试试 ↓` · 4 条建议行。
- 建议行(`.scp0`):`〔tag〕 描述`,tag 用 `accent.primary`。原型 4 条:`任务/给 Config 加 timeout_secs 字段`、`/help/查看内置命令`、`/status/当前会话快照`、`错误/演示:鉴权失败`。
- 渲染:✅ 文本与层级;⚠️ 卡片描边→box 或缩进;letter-spacing→空格。

### C3 · 用户消息(`kind: user`)
- 内容:`> {text}`,marker 用 `accent.primary`。驱动:`UserEchoed`。✅

### C4 · 助手消息(`kind: assistant` · `{text, done}`)
- 驱动:`TextDelta`(流式逐字)→ `TextDone`;流式中状态 `CallingModel`。
- 渲染:✅ 纯文本流式;markdown 渲染留路线图 1.4(❌ 1.0)。

### C5 · 工具卡(`kind: tool`)
- 字段:`{name, args, readonly, status: running|done|error, output[], exit, truncated, extra}`。
- 驱动:`ToolCallStarted`(running)→ `ToolCallFinished`(done / error)。
- **头**:状态 glyph(`running`→spinner / `done`→`✓` / `error`→`✗`)+ `name`(bold,accent)+ `args`(dim);只读卡右侧徽章 `只读 · 自动运行`。
- **体**:`output` 行(等宽 `text.body`);截断时 `⋯ +N 行已截断 (max_output_bytes)`(对应 §5.3 `ToolOutcome.truncated`)。
- **脚**:`exit {code}`(dim;非 0 用 `error.fg`)。
- `error` 态 output 示例:`权限被拒绝 (user denied)`。
- 渲染:✅ 全结构;spinner ⚠️(braille / 点阵帧动画)。

### C6 · 权限框(`pending: {tool, args, diff}` · status `WaitingForPermission`)
- 驱动:`PermissionRequired`(§3 携 `oneshot`)→ `y`/`n` 经 oneshot 回送。
- 位置:钉在状态行上方(与 §8 一致)。`warning` 色框。
- **头**:`▲ 需要授权  {tool}  {args}`。
- **体**(写 / 编辑):**diff**,行类型 `ctx`(dim,前导空格)/ `add`(`success`,`+` gutter)/ `del`(`error`,`-` gutter——原型未演示但实现需有);`run_shell` 类显示命令、无 diff。
- **动作行**:`[y · 允许]   [n · 拒绝]`(按钮→文本 ⚠️)+ 提示 `Enter = 允许 · Esc = 拒绝`。
- **拒绝路径**:tool 卡转 `error` + notice「已拒绝 — 一条 is_error 的 ToolResult 已注入 history,模型据此改路线,而非静默跳过」(§5.4)。

### C7 · 通知 / 横幅(`kind: notice` · `level: info|error`)
- `info`:如 `已拒绝` / `模型已切换` / `/exit`。
- `error`(致命):`error` 框,title `ProviderError::Auth — 致命错误`,text「鉴权失败:未找到 ANTHROPIC_API_KEY。凭据缺失属致命错误(§9),Agent Loop 已终止 —— 不进入重试…」。
- 驱动:`Error` / 致命路径(§9)。✅

### C8 · 帮助块(`kind: help`)
- 内容:7 命令表(`cmd` + `desc`):
  - `/help` — 显示命令列表
  - `/clear` — 清空当前会话
  - `/model [name]` — 查看或切换模型
  - `/status` — 当前会话快照(provider·model·轮次·cwd·消息数)
  - `/exit` — 结束会话
  - `/login` — 配置登录 · 1.0 占位
  - `/logout` — 登出 · 1.0 占位
- 渲染:✅ 两列对齐表格。

### C9 · 状态快照块(`kind: status` · `/status`)
- 字段:`{provider, model, iter, maxIter, cwd, msgs, tools: 7}`。
- 与状态行区别:这是 transcript 里的**一次性快照块**,状态行是**常驻实时**(§8「两个东西」)。✅

### C10 · 状态行 / C11 · 输入框
- 见 `02-布局与交互.md`(状态机 + 键位)。
- 输入框:`mysteries ▸ ` + 占位 `输入任务,或 / 执行命令…` + 右侧 `⏎`。✅
